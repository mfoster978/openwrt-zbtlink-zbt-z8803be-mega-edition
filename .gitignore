# Generated build trees, workflow outputs and Python test caches, not sources.
/openwrt/
# Router captures may contain private network/device identifiers. Diagnostic
# tools and release workflows can generate files here, but never commit them.
/artifacts/
__pycache__/
*.py[cod]
