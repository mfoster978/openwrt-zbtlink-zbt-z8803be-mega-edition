{
  "luci-app-speedtest-lite": {
    "description": "Run and stop Speedtest.net-server tests",
    "read": { "ubus": { "zbt.speedtest": ["status"] } },
    "write": { "ubus": { "zbt.speedtest": ["start", "cancel"] } }
  }
}
