#!/bin/sh
# Mega Edition deliberately does not ship the shared vendor password "admin".
# A fresh image keeps OpenWrt's empty initial root password long enough for the
# first local LuCI login, then the pinned LuCI dispatcher requires the owner to
# set a unique root password before any other administration page is served.
# Existing password hashes restored by sysupgrade are never modified here.
exit 0
